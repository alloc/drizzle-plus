# drizzle-plus documentation

> The human-facing documentation source lives in `docs/content/` and is built with lildocs.

Open the [documentation home](content/index.md) to browse the feature pages.

Build the static site from the repository root:

```bash
pnpm run docs:build
```
